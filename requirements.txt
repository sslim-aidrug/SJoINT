# SJoINT pip requirements.
#
# CONDA IS RECOMMENDED — see environment.yml. This file is a pip-only fallback.
#
# Install order matters because PyTorch Geometric must match your PyTorch +
# CUDA build. Run roughly:
#
#   1) Pick a torch build for your GPU/CUDA from https://pytorch.org/
#         pip install "torch>=2.7" --index-url https://download.pytorch.org/whl/cu124
#   2) Install PyG against that torch:
#         pip install "torch_geometric>=2.7"
#   3) Install the chemistry + numerics stack:
#         pip install -r requirements.txt
#
# Hard pins are intentionally avoided here so users can match their own
# CUDA / driver combination. For exact reproduction see environment-lock.yml.

torch>=2.7
torch_geometric>=2.7
rdkit>=2024.03.1
numpy>=1.24
scipy>=1.11
scikit-learn>=1.3
tqdm>=4.66
